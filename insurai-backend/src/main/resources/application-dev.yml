spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    com.insurai: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG

app:
  frontend-url: http://localhost:5173

JWT_SECRET: insurai_secure_super_long_secret_key_2026_backend_ai_project_development
JWT_EXPIRATION: 86400000